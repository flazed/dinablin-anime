export interface OptionProps {
  value: string | number;
  content: string | number;
}

export enum AnimeSortGroupList {
  Date = "date",
  Rating = "rating"
}

export enum OrderBy {
  DESC = "DESC",
  ASC = "ASC"
}
